# Copy to .env and fill values before running in production
# SQLite file inside container (persisted via docker volume)
DB_FILE=data/blx_banco.db

# JWT secret (change in production)
SECRET_KEY=change_me_to_a_secure_value
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=160
